When fetchWidth is 4, the BOOM frontend is allowed to fetch up to 4 instructions per cycle into the fetch pipeline. This setting defines the instruction supply capability that feeds the fetch target queue and the fetch buffer, and it sets a hard limit on how many new instructions can enter the core each cycle from the frontend. It drives I-cache access activity and predictor lookup activity whenever the frontend is not stalled, with corresponding energy in the frontend structures. This setting requires ICacheFetchBytes to provide enough instruction bytes per cycle to assemble a full fetch bundle when instruction alignment allows. It can be bottlenecked by decodeWidth and numFetchBufferEntries if the downstream stages cannot accept or buffer the delivered instruction stream.

When fetchWidth is 8, the BOOM frontend is allowed to fetch up to 8 instructions per cycle into the fetch pipeline. This setting defines the instruction supply capability that feeds the fetch target queue and the fetch buffer, and it sets a hard limit on how many new instructions can enter the core each cycle from the frontend. It drives intensive I-cache data delivery and branch prediction plumbing, because the frontend must form and steer a large bundle each cycle to sustain fetch. This setting requires ICacheFetchBytes and ICacheWay to deliver enough bytes and hits for the frontend to construct the fetch bundle without frequent bubbles. It can be bottlenecked by decodeWidth and numFetchBufferEntries if decode throughput or buffering capacity cannot absorb the offered fetch bandwidth.

When numFetchBufferEntries is 8, the frontend fetch buffer can hold up to 8 buffered fetch entries awaiting decode and downstream processing. This setting defines the amount of decoupling between instruction fetch and decode, so it determines how quickly fetch must stop when decode or rename applies backpressure. It also defines how much speculative fetched work can sit in the buffer, which affects how much buffered content can be invalidated by redirects and flushes. This setting requires decodeWidth to drain buffered entries at a steady rate to prevent frequent fetch throttling. It can be bottlenecked by maxBrCount and numRasEntries when frequent control-flow redirects invalidate buffered entries and force refetch.

When numFetchBufferEntries is 16, the frontend fetch buffer can hold up to 16 buffered fetch entries awaiting decode and downstream processing. This setting defines the amount of decoupling between instruction fetch and decode, and it provides storage that can keep decode supplied through short downstream stalls. It also creates bookkeeping for valid bits, pointers, and redirect handling, which consumes area and switching energy. This setting requires fetchWidth and ICacheFetchBytes to supply enough instruction data to keep the buffer populated when the core is ready for work. It can be bottlenecked by decodeWidth if decode cannot accept buffered entries at the rate needed to avoid buffer saturation.

When numFetchBufferEntries is 20, the frontend fetch buffer can hold up to 20 buffered fetch entries awaiting decode and downstream processing. This setting defines how much fetched instruction work can accumulate ahead of decode, which influences how long the frontend can continue fetching while decode is temporarily blocked. It also defines how much speculative buffered content may be discarded on a redirect, which affects recovery traffic and refetch demand. This setting requires decodeWidth to provide enough steady consumption so entries do not remain buffered for long periods. It can be bottlenecked by maxBrCount when many unresolved branches cause redirects that invalidate buffered work and force repeated refills.

When numFetchBufferEntries is 24, the frontend fetch buffer can hold up to 24 buffered fetch entries awaiting decode and downstream processing. This setting defines a sizable staging area for fetched bundles, which can keep decode fed across brief I-cache or backend hiccups. It also increases the amount of buffer state that must be checked and updated on redirects, flushes, and exceptions, which has control complexity and energy cost. This setting requires fetchWidth to generate enough useful fetched work to occupy the buffer while staying on the correct path. It can be bottlenecked by decodeWidth and maxBrCount if decode cannot drain entries and redirects repeatedly invalidate the buffered content.

When numFetchBufferEntries is 32, the frontend fetch buffer can hold up to 32 buffered fetch entries awaiting decode and downstream processing. This setting defines substantial fetch/decode decoupling and allows many fetched bundles to wait while rename, dispatch, or the ROB applies backpressure. It also increases the amount of speculative buffer content that can be squashed on a misprediction, which can translate into refetch traffic and recovery activity. This setting requires ICacheWay and ICacheTLB to keep instruction fetch responsive so the buffer can be refilled after redirects. It can be bottlenecked by decodeWidth if decode acceptance limits prevent the buffer from being converted into in-flight backend work.

When numFetchBufferEntries is 40, the frontend fetch buffer can hold up to 40 buffered fetch entries awaiting decode and downstream processing. This setting defines a deep staging pool that can preserve instruction supply to decode across extended downstream pressure. It also increases storage and control-state maintenance for the buffer, including redirect-aware invalidation and pointer management, which affects area and switching. This setting requires fetchWidth and ICacheFetchBytes to sustain enough instruction delivery to keep the buffer populated when the core is ready to run. It can be bottlenecked by decodeWidth and maxBrCount if decode throughput and control-flow recovery prevent steady draining and reuse of buffered entries.

When numRasEntries is 16, the return-address stack (RAS) can hold up to 16 return targets for predicting function returns in the frontend. This setting defines how many nested calls can be tracked with a push/pop mechanism so that RET instructions can be predicted with a concrete target address. It affects frontend redirect frequency on returns because a missing or incorrect return target triggers a control-flow correction and refetch. This setting requires maxBrCount to support the speculative control-flow bookkeeping that accompanies return prediction and recovery. It can be bottlenecked by numFetchBufferEntries if repeated return redirects invalidate buffered fetch entries and force frequent refills.

When numRasEntries is 24, the return-address stack (RAS) can hold up to 24 return targets for predicting function returns in the frontend. This setting defines the depth of call/return nesting that the RAS mechanism can represent using push/pop updates on calls and returns. It affects control-flow stability around function-heavy code because correct return targets reduce the need for pipeline flush and refetch events on RET resolution. This setting requires fetchWidth to refill the frontend quickly after any redirect that still occurs due to return-control uncertainty. It can be bottlenecked by maxBrCount if unresolved branches and returns combine to create frequent redirects that consume recovery bandwidth.

When numRasEntries is 32, the return-address stack (RAS) can hold up to 32 return targets for predicting function returns in the frontend. This setting defines the capacity of the RAS structure that stores return PCs and updates it with call/return events to steer RET fetch. It influences the rate of return-induced frontend flushes because return prediction accuracy directly affects whether the frontend stays on the correct path. This setting requires ICacheTLB to supply translations for return targets promptly, since return targets can jump across pages and still demand quick fetch. It can be bottlenecked by maxBrCount and numFetchBufferEntries if control-flow recovery repeatedly invalidates buffered instructions and stalls the fetch pipeline.

When maxBrCount is 8, the core is allowed to track up to 8 unresolved branches in flight using branch snapshots and speculation bookkeeping. This setting defines how many branch checkpoints can exist concurrently, including rename-map state and related metadata needed to recover on a misprediction. It affects how often fetch, decode, or dispatch must stall due to running out of branch-tracking resources when many branches have not yet resolved. This setting requires numRobEntries to hold the in-flight instructions that sit behind unresolved branches until resolution and retirement. It can be bottlenecked by IntIssueWidth and IntDispatchWidth if integer execution throughput does not resolve branch-producing operations quickly enough.

When maxBrCount is 12, the core is allowed to track up to 12 unresolved branches in flight using branch snapshots and speculation bookkeeping. This setting defines the number of concurrent speculative branch checkpoints that can be active, which includes recovery state for rename and control-flow tracking. It affects recovery work on a misprediction because the core must restore the checkpoint for the mispredicted branch and invalidate younger instructions. This setting requires numRobEntries and numIntPhysRegisters to provide enough in-flight and rename resources to keep useful work moving while branches are pending. It can be bottlenecked by fetchWidth and ICacheWay if frontend delivery and I-cache behavior cannot keep the pipeline supplied through branchy control flow.

When maxBrCount is 16, the core is allowed to track up to 16 unresolved branches in flight using branch snapshots and speculation bookkeeping. This setting defines how many branch checkpoints can be maintained concurrently and therefore how much speculative control flow can be outstanding before the core must wait for resolution. It affects control-heavy workloads by shaping how often the machine experiences branch-resource backpressure and how much speculative state must be managed at once. This setting requires numRobEntries to accommodate the in-flight instruction window behind outstanding branches without forcing frequent decode stalls. It can be bottlenecked by IntNumEntries and IntIssueWidth if the integer scheduler and execution pipeline cannot drive branch resolution at a sufficient pace.

When maxBrCount is 20, the core is allowed to track up to 20 unresolved branches in flight using branch snapshots and speculation bookkeeping. This setting defines the number of speculative control-flow checkpoints that can exist concurrently, including state needed to roll back rename and frontend control. It affects flush and recovery behavior because a misprediction triggers a checkpoint restore and invalidation of younger speculative work that was tracked under these checkpoints. This setting requires numRobEntries and numFetchBufferEntries to manage a large amount of in-flight and buffered speculative work without deadlocking the pipeline. It can be bottlenecked by ICacheTLB and ICacheFetchBytes if redirects and refetches stress translation and I-cache delivery on rapid path changes.

When ICacheWay is 2, the L1 instruction cache is configured as 2-way set-associative, meaning each set maintains two tags and two data ways for hit selection. This setting defines how instruction lines compete for residency within a set, and it determines that each access performs up to 2 tag comparisons to select a way. It impacts frontend stalls when instruction working sets map into the same sets and cause replacement and refetch activity, which translates into fetch bubbles. This setting requires ICacheTLB to provide timely address translation so I-cache access can proceed without translation stalls. It can be bottlenecked by fetchWidth and ICacheFetchBytes if the frontend requests instruction bandwidth that the I-cache hit/miss behavior cannot sustain.

When ICacheWay is 4, the L1 instruction cache is configured as 4-way set-associative, meaning each set maintains four tags and four data ways for hit selection. This setting defines that each access performs up to 4 tag comparisons and selects one of four resident lines for instruction delivery. It impacts instruction fetch continuity by shaping eviction and hit behavior, and it consumes dynamic energy in tag and data array activity during accesses. This setting requires ICacheFetchBytes to deliver enough bytes per access for the frontend to assemble fetched instructions efficiently. It can be bottlenecked by ICacheTLB if translation misses stall the I-cache pipeline even when data would otherwise hit.

When ICacheWay is 8, the L1 instruction cache is configured as 8-way set-associative, meaning each set maintains eight tags and eight data ways for hit selection. This setting defines that each access performs up to 8 tag comparisons and then drives a selected way’s data for instruction delivery. It affects power and timing because the tag match and way selection logic must operate across eight ways, and the data array must support that selection each access. This setting requires fetchWidth and ICacheFetchBytes to be aligned with the I-cache’s ability to deliver instruction bytes at the demanded rate. It can be bottlenecked by numFetchBufferEntries and decodeWidth if the downstream pipeline cannot absorb the delivered instructions even when the I-cache hits.

When ICacheFetchBytes is 8, the instruction cache frontend interface delivers 8 bytes per fetch access for building the fetch bundle. This setting defines the byte bandwidth available per cycle from the I-cache, which directly affects how many instructions can be formed depending on instruction size and alignment. It impacts frontend bubbles when the fetch bundle needs bytes that span boundaries, because the frontend may need extra cycles to assemble the needed instruction bytes. This setting requires fetchWidth to be consistent with the available byte supply so the frontend does not request instruction slots that cannot be filled from delivered bytes. It can be bottlenecked by decodeWidth if decode cannot consume the formed instructions and causes the frontend to throttle.

When ICacheFetchBytes is 16, the instruction cache frontend interface delivers 16 bytes per fetch access for building the fetch bundle. This setting defines the byte bandwidth available per cycle from the I-cache and supports forming multiple instructions without immediate boundary penalties when alignment permits. It impacts frontend energy because the I-cache data path and associated muxing must deliver and route 16 bytes per access into the instruction assembly logic. This setting requires fetchWidth and numFetchBufferEntries to provide and absorb instruction supply so delivered bytes translate into useful in-flight work. It can be bottlenecked by ICacheTLB if translation stalls prevent the I-cache from producing the 16-byte data beats on time.

When ICacheTLB is 8, the instruction-side TLB structure is configured with 8 ways in its L1 translation lookup for fetch addresses. This setting defines how many translation entries can be checked in parallel for an instruction fetch, and it determines how often instruction fetch must wait on a translation miss and subsequent page-walk handling. It impacts frontend throughput because a translation miss stalls instruction fetch even if the instruction cache tags and data would hit. This setting requires fetchWidth to present fetch PCs at a pace the TLB can translate without frequent stalls, especially under rapid control-flow changes. It can be bottlenecked by maxBrCount and numRasEntries if redirects cause fetch to touch many distinct pages that stress the translation working set.

When ICacheTLB is 16, the instruction-side TLB structure is configured with 16 ways in its L1 translation lookup for fetch addresses. This setting defines the translation lookup capacity used to translate instruction fetch PCs and influences how many distinct instruction pages can be kept resident in the L1 instruction translation structure. It impacts frontend stability by reducing the need for page-walk activity when the working set of code spans multiple pages. This setting requires ICacheWay and ICacheFetchBytes to sustain instruction delivery so translation availability converts into continuous fetch. It can be bottlenecked by numFetchBufferEntries if downstream backpressure forces fetch to stop frequently and then restart with redirects that still trigger translation churn.

When ICacheTLB is 32, the instruction-side TLB structure is configured with 32 ways in its L1 translation lookup for fetch addresses. This setting defines the translation lookup capacity for instruction fetch PCs and supports keeping a sizable set of page translations available for fast fetch. It impacts energy and area because the TLB match and metadata structures must operate across 32 ways during translation checks. This setting requires fetchWidth and ICacheFetchBytes to provide enough instruction fetch demand so translation capacity is exercised efficiently by the frontend. It can be bottlenecked by decodeWidth and numFetchBufferEntries if the backend cannot accept sustained fetch and the frontend spends cycles idle despite ready translations.

When decodeWidth is 1, the decode stage can decode up to 1 instruction per cycle and send it into rename and dispatch. This setting defines the maximum frontend-to-backend intake rate and sets a throughput ceiling on how quickly new in-flight work can be created. It impacts backpressure because the fetch buffer and frontend must align to a single-instruction decode acceptance rate, or they will spend cycles stalled or buffering. This setting requires fetchWidth and numFetchBufferEntries to supply at least one decodable instruction each cycle without starvation. It can be bottlenecked by numRobEntries if long-latency operations keep the ROB occupied and prevent steady retirement.

When decodeWidth is 2, the decode stage can decode up to 2 instructions per cycle and send them into rename and dispatch. This setting defines a two-instruction intake capability that drives rename allocation, ROB occupancy growth, and dispatch traffic into the backend. It impacts structural pressure on rename resources and queues because two instructions per cycle can allocate physical registers, ROB entries, and issue-queue entries at a sustained rate. This setting requires numIntPhysRegisters and numFpPhysRegisters to supply enough renameable registers so decode does not stall on free-list exhaustion. It can be bottlenecked by IntDispatchWidth and MemDispatchWidth if dispatch ports cannot accept decoded operations at the offered rate.

When decodeWidth is 3, the decode stage can decode up to 3 instructions per cycle and send them into rename and dispatch. This setting defines a three-instruction intake capability and drives steady allocation into the ROB and backend scheduling structures. It impacts recovery and control complexity because multiple instructions can enter speculative state each cycle, and redirects can invalidate multiple younger instructions in a single cycle’s intake. This setting requires numRobEntries to hold the in-flight instruction set created by sustained decode without forcing frequent stalls. It can be bottlenecked by IntNumEntries, MemNumEntries, and FpNumEntries if issue queues cannot provide enough capacity to accept and schedule decoded work.

When decodeWidth is 4, the decode stage can decode up to 4 instructions per cycle and send them into rename and dispatch. This setting defines a four-instruction intake capability and drives high allocation demand in rename, the ROB, and the issue queues. It impacts power because decode, rename mapping, and dispatch steering must process four instructions per cycle when the pipeline is active. This setting requires numIntPhysRegisters and numFpPhysRegisters to provide sufficient physical-register allocation for renamed destinations. It can be bottlenecked by IntDispatchWidth, MemDispatchWidth, and FpDispatchWidth if dispatch cannot route four instructions per cycle into the appropriate issue queues.

When decodeWidth is 5, the decode stage can decode up to 5 instructions per cycle and send them into rename and dispatch. This setting defines a five-instruction intake capability and pushes high per-cycle demand through decode, rename map tables, and dispatch steering logic. It impacts queueing pressure because the ROB and issue queues must accept frequent bursts of newly decoded work without creating structural stalls. This setting requires numRobEntries and the issue-queue capacities MemNumEntries, IntNumEntries, and FpNumEntries to provide enough buffering for the incoming stream. It can be bottlenecked by MemDispatchWidth, IntDispatchWidth, and FpDispatchWidth if the dispatch fabric cannot accept and place the decoded operations.

When numRobEntries is 32, the reorder buffer can hold up to 32 in-flight instructions for precise exception handling and in-order commit. This setting defines the maximum number of speculative instructions that can be tracked at once, including completion status and commit ordering. It impacts tolerance to cache misses and long-latency execution because instructions waiting on events occupy ROB slots until they can complete and retire. This setting requires decodeWidth to manage ROB allocation so decode stalls occur cleanly when the ROB is full. It can be bottlenecked by MemNumEntries and MemIssueWidth if memory operations cannot complete and drain from the ROB in a timely way.

When numRobEntries is 64, the reorder buffer can hold up to 64 in-flight instructions for precise exception handling and in-order commit. This setting defines the speculative instruction window that can be tracked concurrently, including commit bookkeeping and recovery metadata. It impacts throughput under latency because the core can keep independent instructions in flight while some wait for memory or multi-cycle execution, subject to other resources. This setting requires numIntPhysRegisters and numFpPhysRegisters to supply enough physical registers so rename does not stall while filling the ROB. It can be bottlenecked by numLdqEntries and numStqEntries if memory operations accumulate faster than the LSU queues can track and execute them.

When numRobEntries is 96, the reorder buffer can hold up to 96 in-flight instructions for precise exception handling and in-order commit. This setting defines a deep in-flight tracking structure that retains completion state, exception state, and commit order for up to 96 instructions. It impacts recovery behavior because mispredictions and exceptions require invalidating younger ROB entries and restoring architectural state at the precise commit boundary. This setting requires issue-queue capacities IntNumEntries, MemNumEntries, and FpNumEntries to provide enough scheduling storage so a populated ROB translates into executable work. It can be bottlenecked by numIntPhysRegisters and numFpPhysRegisters if rename allocation cannot keep up with the instruction window’s demand for live destinations.

When numRobEntries is 128, the reorder buffer can hold up to 128 in-flight instructions for precise exception handling and in-order commit. This setting defines the maximum tracked speculative footprint and includes ROB storage, commit pointer logic, and per-entry status tracking. It impacts area and energy because the ROB must maintain tags, ready bits, exception flags, and commit eligibility for each entry. This setting requires IntDispatchWidth and MemDispatchWidth to deliver sufficient backend work so ROB occupancy can be converted into completed instructions. It can be bottlenecked by numLdqEntries and DCacheMSHR if memory operations occupy the ROB while waiting on cache misses and miss-handling resources.

When numRobEntries is 130, the reorder buffer can hold up to 130 in-flight instructions for precise exception handling and in-order commit. This setting defines the tracked in-flight instruction set size and requires commit logic to retire instructions in order while preserving precise exceptions. It impacts control and recovery because redirects and exceptions must invalidate younger ROB entries and keep older entries’ architectural effects consistent. This setting requires numIntPhysRegisters and numFpPhysRegisters to support the live-value pressure created by many in-flight renamed destinations. It can be bottlenecked by MemIssueWidth and DCacheMSHR if memory completion cannot keep pace and ROB entries remain occupied by waiting loads and stores.

When numIntPhysRegisters is 48, the integer physical register file provides 48 renameable integer registers for BOOM’s register renaming. This setting defines how many distinct integer destination values can remain live across speculative execution before the integer free list runs out and stalls rename. It impacts throughput in integer-heavy code because integer destination allocation is required for most integer-producing instructions, and a stall at rename stops new work from entering the backend. This setting requires numRobEntries to avoid creating an in-flight window that demands more live integer destinations than the physical register pool can sustain. It can be bottlenecked by decodeWidth and IntDispatchWidth if they inject integer-producing instructions rapidly and retirement does not recycle registers promptly.

When numIntPhysRegisters is 64, the integer physical register file provides 64 renameable integer registers for BOOM’s register renaming. This setting defines the integer renaming capacity and governs how long dependency chains can stay live across speculation without exhausting integer rename resources. It impacts physical register file access activity because reads, writes, and wakeups involve this pool whenever integer operations execute and write back. This setting requires numRobEntries to manage instruction lifetimes so registers are freed at commit with a steady cadence. It can be bottlenecked by maxBrCount if frequent redirects delay retirement and keep integer physical registers allocated to speculative destinations.

When numIntPhysRegisters is 96, the integer physical register file provides 96 renameable integer registers for BOOM’s register renaming. This setting defines a sizable rename pool that supports keeping many integer destinations live while out-of-order execution proceeds. It impacts area and energy because the register file storage, read ports, write ports, and bypass network must support the physical register count and the execution pipeline’s usage. This setting requires IntNumEntries and IntIssueWidth to schedule and execute enough integer work so live destinations progress toward completion and eventual retirement. It can be bottlenecked by numRobEntries if commit throughput constraints keep completed instructions in the ROB and delay register reclamation.

When numIntPhysRegisters is 112, the integer physical register file provides 112 renameable integer registers for BOOM’s register renaming. This setting defines integer rename capacity that can support many simultaneously live integer values across a deep speculative footprint. It impacts timing and power because mapping tables, free lists, and physical register file structures must track and service this many registers under active execution. This setting requires decodeWidth and IntDispatchWidth to coordinate allocation pressure so rename does not thrash on free-list management. It can be bottlenecked by numRobEntries and maxBrCount if long speculative sequences and redirects delay retirement and keep integer physical registers allocated for extended periods.

When numFpPhysRegisters is 48, the floating-point physical register file provides 48 renameable FP registers for BOOM’s FP renaming. This setting defines how many FP destination values can remain live concurrently before FP rename stalls due to free-list exhaustion. It impacts FP throughput because FP-producing instructions must allocate a physical FP register, and a stall in FP rename can block dispatch of FP operations and dependent instructions. This setting requires FpDispatchWidth and FpNumEntries to place FP operations into the FP issue queue without immediate structural stalls. It can be bottlenecked by numRobEntries if FP operations with long completion latency occupy ROB entries and delay freeing FP registers at commit.

When numFpPhysRegisters is 64, the floating-point physical register file provides 64 renameable FP registers for BOOM’s FP renaming. This setting defines the FP rename capacity that supports multiple live FP dependency chains during out-of-order execution. It impacts area and energy because FP register storage and access ports must support the register count while serving operand reads and writebacks. This setting requires FpIssueWidth to issue FP operations at a steady rate so live destinations move toward completion and retirement. It can be bottlenecked by decodeWidth if the instruction stream injects FP-producing operations that cannot be dispatched due to FpDispatchWidth or FP queue pressure.

When numFpPhysRegisters is 96, the floating-point physical register file provides 96 renameable FP registers for BOOM’s FP renaming. This setting defines a substantial FP rename pool that can keep many FP destinations live during speculative execution and scheduling. It impacts power because FP rename bookkeeping and physical register file accesses occur frequently in FP-heavy regions, including wakeup and writeback activity. This setting requires FpNumEntries and FpDispatchWidth to buffer and route FP operations into scheduling without causing immediate backpressure to rename. It can be bottlenecked by numRobEntries if commit order constraints delay retirement and hold FP registers allocated to completed-but-not-retired instructions.

When numFpPhysRegisters is 112, the floating-point physical register file provides 112 renameable FP registers for BOOM’s FP renaming. This setting defines the FP live-value capacity that can support many in-flight FP destinations in an out-of-order window. It impacts implementation cost because the FP map tables, free lists, and physical register file structures must track and service 112 registers with correct dependency tracking. This setting requires FpIssueWidth to keep FP pipelines active so the live FP register set represents useful forward progress rather than stalled operations. It can be bottlenecked by FpDispatchWidth and FpNumEntries if dispatch or queue capacity prevents FP operations from reaching execution and freeing rename pressure via retirement.

When numLdqEntries is 8, the load queue can hold up to 8 in-flight loads for memory ordering, dependency tracking, and replay support. This setting defines how many speculative loads can be tracked concurrently while the core checks for store-to-load forwarding conditions and detects ordering violations. It impacts memory-level overlap because loads that are waiting on cache misses, translation, or dependencies occupy load-queue slots until they complete. This setting requires DCacheMSHR to provide enough miss-tracking capability so outstanding load misses can make forward progress without blocking new loads. It can be bottlenecked by MemDispatchWidth and MemIssueWidth if memory operations are injected and scheduled faster than the LSU can complete and retire them.

When numLdqEntries is 16, the load queue can hold up to 16 in-flight loads for memory ordering, dependency tracking, and replay support. This setting defines the load tracking capacity that the LSU uses to manage speculative execution of loads and to coordinate with the store queue for forwarding and ordering checks. It impacts replay behavior because the LSU may need to re-execute loads when hazards are detected, and each tracked load carries metadata for such checks. This setting requires DCacheTLB to translate load addresses promptly so loads can access the cache pipeline without translation stalls. It can be bottlenecked by numStqEntries if store pressure and forwarding checks occupy LSU resources and prevent loads from completing and draining.

When numLdqEntries is 24, the load queue can hold up to 24 in-flight loads for memory ordering, dependency tracking, and replay support. This setting defines the capacity for tracking many speculative loads and maintaining their addresses, ages, and completion status. It impacts LSU complexity because ordering checks and forwarding searches must consider a larger set of tracked loads and their interactions with stores. This setting requires numDCacheBanks to provide enough cache access throughput so issued loads can be serviced without persistent bank conflicts. It can be bottlenecked by DCacheMSHR if cache-miss loads occupy load-queue entries while waiting for miss-handling resources and memory responses.

When numLdqEntries is 32, the load queue can hold up to 32 in-flight loads for memory ordering, dependency tracking, and replay support. This setting defines a large load tracking structure that retains per-load metadata until completion and retirement conditions are satisfied. It impacts power and area because the load queue stores address and status fields and participates in hazard detection and replay logic. This setting requires MemNumEntries and MemIssueWidth to schedule load operations efficiently so the queue drains with steady completion. It can be bottlenecked by DCacheTLB and DCacheMSHR if translation stalls and miss handling prevent loads from completing and releasing load-queue entries.

When numStqEntries is 8, the store queue can hold up to 8 in-flight stores for ordering, forwarding, and commit to the data cache. This setting defines how many stores can wait for address/data readiness and retirement while maintaining correct memory ordering and enabling store-to-load forwarding. It impacts dispatch backpressure because stores occupy store-queue slots until they can commit, and a full store queue prevents new stores from being dispatched. This setting requires DCacheMSHR to handle store-related cache misses and coherence activity so committed stores can drain to the cache system. It can be bottlenecked by MemIssueWidth and numDCacheBanks if the cache pipeline and LSU scheduling cannot service store address/data operations at a steady pace.

When numStqEntries is 16, the store queue can hold up to 16 in-flight stores for ordering, forwarding, and commit to the data cache. This setting defines the store buffering capacity for speculative execution, including metadata used for forwarding checks and commit eligibility. It impacts LSU activity because each load may need to search the store queue for forwarding opportunities and ordering hazards, which consumes energy and control effort. This setting requires numLdqEntries to coordinate load/store interactions without losing ordering information, since loads and stores are checked against each other. It can be bottlenecked by DCacheTLB if store address translation stalls prevent stores from becoming ready to commit and drain from the store queue.

When numStqEntries is 24, the store queue can hold up to 24 in-flight stores for ordering, forwarding, and commit to the data cache. This setting defines a sizable store tracking structure that retains store addresses, data readiness, and age information until retirement and cache commit occur. It impacts commit behavior because stores must commit in order and may wait behind older instructions in the ROB, keeping store-queue entries occupied. This setting requires numRobEntries to track the associated in-flight store instructions and ensure they can reach retirement so stores can be released to the memory system. It can be bottlenecked by numDCacheBanks and DCacheMSHR if cache access and miss-handling resources prevent committed stores from draining.

When numStqEntries is 32, the store queue can hold up to 32 in-flight stores for ordering, forwarding, and commit to the data cache. This setting defines extensive store buffering capacity and retains store metadata used for forwarding to younger loads and for ordering checks. It impacts power because store-queue CAM-like searches and hazard checks can be exercised frequently during load execution and store commit. This setting requires MemDispatchWidth to inject stores at a rate that the LSU can ultimately service without persistent store-queue fullness. It can be bottlenecked by DCacheTLB and DCacheMSHR if translation stalls and miss handling prevent stores from committing to the cache system and freeing entries.

When numDCacheBanks is 1, the L1 data cache is organized as a single bank for the LSU’s cache access path. This setting defines that cache pipeline bandwidth is constrained to one banked access domain, so concurrent loads and stores contend for the same bank resource when accessing the cache. It impacts memory throughput by introducing bank-conflict stalls when the LSU issues multiple memory operations that must be serviced by the single bank. This setting requires MemIssueWidth to schedule memory operations in a way that the cache pipeline can accept without persistent structural stalls. It can be bottlenecked by numLdqEntries and numStqEntries if bank conflicts prevent loads and stores from completing and draining the LSU queues.

When numDCacheBanks is 2, the L1 data cache is organized as two banks for the LSU’s cache access path. This setting defines that cache accesses can proceed concurrently when they map to different banks, subject to the cache’s banking and arbitration rules. It impacts LSU throughput by enabling dual-bank servicing of memory operations when bank mapping aligns with the issued accesses. This setting requires MemIssueWidth and MemDispatchWidth to provide a steady stream of memory operations so bank resources are exercised effectively. It can be bottlenecked by DCacheMSHR if cache misses dominate and miss-handling resources, rather than bank availability, constrain completion.

When DCacheWay is 2, the L1 data cache is configured as 2-way set-associative for tag/data storage and hit selection. This setting defines that each access performs up to 2 tag comparisons to determine a hit and select a resident line for load/store servicing. It impacts cache miss behavior and therefore load latency and store commit latency, because misses trigger refills and may stall dependent operations. This setting requires DCacheMSHR to track miss transactions so the core can continue making progress on other work while a refill is pending. It can be bottlenecked by MemIssueWidth if the LSU cannot issue memory operations at a pace that keeps the cache pipeline active without saturating miss handling.

When DCacheWay is 4, the L1 data cache is configured as 4-way set-associative for tag/data storage and hit selection. This setting defines that each access performs up to 4 tag comparisons and selects one of four resident lines to service a load or store hit. It impacts energy because tag comparisons and way selection logic activate across four ways on access, and it impacts throughput when misses and replacements drive refill and writeback activity. This setting requires DCacheTLB to translate addresses promptly so cache tag/data lookup can proceed without translation stalls. It can be bottlenecked by numDCacheBanks if cache access concurrency is limited by banking contention even when hits occur.

When DCacheWay is 8, the L1 data cache is configured as 8-way set-associative for tag/data storage and hit selection. This setting defines that each access performs up to 8 tag comparisons and uses way selection logic across eight ways to deliver the correct cache line. It impacts implementation cost and energy because the tag arrays, comparators, and data muxing must support eight-way operation at cache timing. This setting requires numDCacheBanks to provide enough banking throughput so the LSU can present accesses that make use of cache hits without frequent structural blocking. It can be bottlenecked by DCacheMSHR if miss concurrency and refill processing become the dominant limiter for memory completion.

When DCacheMSHR is 2, the data cache is configured with 2 MSHRs, allowing up to 2 outstanding cache misses to be tracked concurrently. This setting defines miss-handling concurrency for refills and coherence transactions, and it determines when new misses must stall or merge due to unavailable MSHR state. It impacts load and store progress because misses that cannot allocate an MSHR prevent the cache pipeline from accepting new miss-generating accesses. This setting requires MemIssueWidth to schedule memory operations in a way that does not deadlock the pipeline on repeated misses. It can be bottlenecked by numLdqEntries if load-queue entries remain occupied by miss loads waiting for MSHR allocation and response completion.

When DCacheMSHR is 4, the data cache is configured with 4 MSHRs, allowing up to 4 outstanding cache misses to be tracked concurrently. This setting defines how many independent miss transactions can be in flight, including state for refills, writebacks, and coherence tracking. It impacts memory-level overlap because multiple miss loads can proceed to allocate MSHRs and wait for responses while other operations execute. This setting requires MemNumEntries to buffer memory operations that are ready to issue while miss responses are pending. It can be bottlenecked by DCacheTLB if translation misses prevent memory operations from reaching the cache pipeline to allocate and use MSHRs.

When DCacheMSHR is 8, the data cache is configured with 8 MSHRs, allowing up to 8 outstanding cache misses to be tracked concurrently. This setting defines substantial miss-handling concurrency and keeps miss state for multiple in-flight refills and coherence actions. It impacts area and energy because each MSHR maintains tags, way information, request metadata, and response tracking until completion. This setting requires MemIssueWidth and numDCacheBanks to generate and service cache accesses at a pace that meaningfully uses the available miss concurrency. It can be bottlenecked by numRobEntries if many miss-dependent instructions remain in flight and block retirement, keeping resources occupied despite active miss handling.

When DCacheTLB is 8, the data-side TLB structure is configured with 8 ways in its L1 translation lookup for load and store addresses. This setting defines the translation lookup capability used by the LSU before accessing the data cache, and it determines how often memory operations stall on translation misses and page-walk activity. It impacts load-use latency and store address generation because untranslated addresses cannot proceed through cache tag/data access in the normal pipeline. This setting requires MemIssueWidth to schedule memory operations in a way that respects translation availability and avoids flooding the LSU with untranslated requests. It can be bottlenecked by numLdqEntries and numStqEntries if translation stalls prevent the LSU queues from draining and accepting new memory operations.

When DCacheTLB is 16, the data-side TLB structure is configured with 16 ways in its L1 translation lookup for load and store addresses. This setting defines the translation lookup capability for memory accesses and supports keeping a substantial set of translations resident for LSU use. It impacts steady-state memory throughput by reducing the frequency of translation-driven stalls in the cache access path when the working set spans multiple pages. This setting requires DCacheMSHR to handle miss traffic that occurs after translation, since translated accesses can still miss in the cache and allocate miss state. It can be bottlenecked by numDCacheBanks if banking contention limits cache access throughput even when translations are readily available.

When DCacheTLB is 32, the data-side TLB structure is configured with 32 ways in its L1 translation lookup for load and store addresses. This setting defines the translation lookup capability for LSU memory operations and enables fast translation checks across a large set of resident translations. It impacts energy and area because the TLB match structures and metadata must operate across 32 ways for each translation lookup. This setting requires MemDispatchWidth and MemIssueWidth to supply and schedule enough memory operations so translation capacity supports useful cache access activity. It can be bottlenecked by DCacheMSHR if miss tracking and refill handling constrain memory completion despite readily available translations.

When MemIssueWidth is 1, the memory issue queue is allowed to issue up to 1 memory operation per cycle toward the LSU execution path. This setting defines the scheduler’s per-cycle launch capability for loads, stores, and related memory micro-ops that are placed in the memory issue queue. It impacts memory throughput because only one eligible memory operation can be launched each cycle, which shapes cache access utilization and LSU queue draining. This setting requires MemNumEntries to buffer pending memory operations so the single issue slot can be kept busy when dependencies allow. It can be bottlenecked by numDCacheBanks and DCacheMSHR if cache access constraints and miss handling prevent issued operations from completing and freeing space for new ones.

When MemIssueWidth is 2, the memory issue queue is allowed to issue up to 2 memory operations per cycle toward the LSU execution path. This setting defines a dual-launch capability for memory operations, enabling two eligible memory micro-ops to start in the same cycle when ready. It impacts cache pipeline pressure because two issued operations can demand translation, cache access, and miss handling concurrently. This setting requires numDCacheBanks to provide enough cache access bandwidth so two issued operations can be serviced without persistent bank conflicts. It can be bottlenecked by DCacheMSHR if miss-handling resources cannot track the miss traffic created by issued memory operations.

When MemNumEntries is 16, the memory issue queue can hold up to 16 pending memory micro-ops waiting for scheduling and issue. This setting defines the buffering capacity between dispatch and memory execution, including storage for operand readiness and scheduling metadata. It impacts dispatch backpressure because a full memory issue queue blocks further dispatch of memory-class operations until entries issue and retire from the queue. This setting requires MemIssueWidth to drain the queue at a steady cadence so pending operations do not accumulate without bound. It can be bottlenecked by MemDispatchWidth if dispatch injects memory operations into the queue faster than the queue can issue them through the LSU.

When MemNumEntries is 32, the memory issue queue can hold up to 32 pending memory micro-ops waiting for scheduling and issue. This setting defines a sizable pool of memory operations that can wait on dependencies, translation, and cache readiness while remaining visible to the scheduler. It impacts scheduling activity because wakeup and selection logic must track readiness across up to 32 entries, and it impacts energy through that wakeup/select activity. This setting requires DCacheTLB to translate memory addresses promptly so queued operations become eligible to issue without prolonged translation blocking. It can be bottlenecked by MemIssueWidth and DCacheMSHR if issue bandwidth and miss handling do not complete memory operations quickly enough to free queue entries.

When MemNumEntries is 64, the memory issue queue can hold up to 64 pending memory micro-ops waiting for scheduling and issue. This setting defines deep buffering for memory operations and retains readiness and dependency state for a large set of pending loads and stores. It impacts area and power because the queue storage and wakeup/select mechanisms must support 64 tracked entries in the scheduler. This setting requires MemIssueWidth and numLdqEntries/numStqEntries to sustain forward progress so queued operations do not remain blocked behind LSU structural limits. It can be bottlenecked by DCacheMSHR if many queued operations miss and miss-handling state becomes the limiting resource for issuing and completing memory requests.

When MemDispatchWidth is 1, the dispatch stage can send up to 1 memory-class micro-op per cycle into the memory issue queue. This setting defines the intake bandwidth for memory operations into the memory scheduler, including loads and stores steered to the memory issue queue. It impacts overall throughput for memory-heavy code because memory operations beyond this dispatch capacity must wait even if decode and rename can produce them. This setting requires MemNumEntries to buffer dispatched memory operations so transient bursts do not immediately stall dispatch. It can be bottlenecked by decodeWidth if the pipeline composition generates memory operations that contend with other classes and reduce steady dispatch into the memory queue.

When MemDispatchWidth is 2, the dispatch stage can send up to 2 memory-class micro-ops per cycle into the memory issue queue. This setting defines two-per-cycle intake into the memory scheduling domain and shapes how quickly the LSU-facing scheduler is populated. It impacts queue pressure because two memory operations per cycle can fill the memory issue queue and LSU structures if completion is delayed by cache or translation latency. This setting requires DCacheTLB to translate addresses promptly so dispatched operations can become eligible to issue without prolonged blocking. It can be bottlenecked by MemIssueWidth if the scheduler cannot issue dispatched memory operations quickly enough to prevent queue occupancy from saturating.

When MemDispatchWidth is 3, the dispatch stage can send up to 3 memory-class micro-ops per cycle into the memory issue queue. This setting defines the memory-op dispatch bandwidth and allows rapid population of the memory scheduling structure with load/store work. It impacts LSU backpressure because a large volume of dispatched memory operations can quickly occupy the memory issue queue and then the load/store queues. This setting requires numLdqEntries and numStqEntries to track the resulting in-flight loads and stores without immediate structural stalls. It can be bottlenecked by DCacheMSHR if miss traffic generated by dispatched memory operations cannot be tracked and serviced concurrently.

When MemDispatchWidth is 4, the dispatch stage can send up to 4 memory-class micro-ops per cycle into the memory issue queue. This setting defines a high memory dispatch capability and drives aggressive allocation into the memory scheduler whenever the instruction stream contains many memory operations. It impacts cache pressure because dispatched operations that become eligible can issue in bursts and create significant demand on DCacheTLB, numDCacheBanks, and miss handling. This setting requires MemNumEntries to provide enough queue capacity so dispatch can place operations while they wait for readiness and execution. It can be bottlenecked by MemIssueWidth and numDCacheBanks if the LSU cannot issue and service memory operations at the offered dispatch rate.

When MemDispatchWidth is 5, the dispatch stage can send up to 5 memory-class micro-ops per cycle into the memory issue queue. This setting defines very high memory dispatch bandwidth and drives rapid occupancy growth in the memory scheduling and LSU tracking structures. It impacts recovery behavior because memory operations that are speculative may be squashed on redirects, and high dispatch can place many speculative memory ops into the queue before a redirect resolves. This setting requires maxBrCount to manage speculative control-flow depth so dispatched speculative memory operations do not accumulate behind unresolved branches without resolution. It can be bottlenecked by DCacheMSHR and numLdqEntries/numStqEntries if miss handling and LSU queue capacity prevent memory operations from completing and freeing scheduling entries.

When IntIssueWidth is 1, the integer issue queue is allowed to issue up to 1 integer operation per cycle to the integer execution units. This setting defines the integer scheduler’s launch capability for ALU, branch, and integer address-generation-related operations that are categorized as integer-class. It impacts dependency resolution because only one integer op can begin execution per cycle, shaping how quickly integer result values and branch outcomes become available. This setting requires IntNumEntries to keep a pool of integer operations buffered so the issue slot has eligible work when dependencies permit. It can be bottlenecked by IntDispatchWidth if integer operations cannot be dispatched into the issue queue at a steady pace.

When IntIssueWidth is 2, the integer issue queue is allowed to issue up to 2 integer operations per cycle to the integer execution units. This setting defines dual launch capability for integer operations and enables issuing two eligible integer ops in the same cycle when ready. It impacts scheduling pressure because wakeup/select logic must choose and dispatch two operations per cycle, and writeback and bypass activity occurs for both operations as they execute. This setting requires numIntPhysRegisters to support the live-value set created by issuing and completing many integer-producing instructions. It can be bottlenecked by IntNumEntries if the integer queue does not hold enough ready operations to supply two issue slots consistently.

When IntIssueWidth is 3, the integer issue queue is allowed to issue up to 3 integer operations per cycle to the integer execution units. This setting defines triple issue capability for integer operations, which can drive multiple ALU results and branch computations through the integer pipeline in a single cycle. It impacts power because operand reads, bypass selection, and writeback activity can occur for three integer operations per cycle when the pipeline sustains issue. This setting requires IntDispatchWidth to deliver enough integer-class micro-ops into the integer queue to keep the scheduler supplied. It can be bottlenecked by numRobEntries if commit and completion constraints prevent the in-flight window from sustaining enough independent integer operations.

When IntIssueWidth is 4, the integer issue queue is allowed to issue up to 4 integer operations per cycle to the integer execution units. This setting defines four-per-cycle integer launch capability and drives heavy utilization of integer execution resources when ready operations exist. It impacts backend complexity because register file accesses, bypass network selection, and completion bookkeeping must support four concurrent integer executions per cycle. This setting requires IntNumEntries to buffer a sufficient set of candidate integer operations with readiness tracking and selection metadata. It can be bottlenecked by numIntPhysRegisters if rename stalls limit the supply of integer producers and consumers entering the integer scheduler.

When IntIssueWidth is 5, the integer issue queue is allowed to issue up to 5 integer operations per cycle to the integer execution units. This setting defines five-per-cycle integer launch capability and can drive a high volume of integer execution and writeback activity per cycle. It impacts scheduling and wiring complexity because operand delivery, bypass selection, and completion reporting must support five issued operations in a single cycle. This setting requires IntDispatchWidth and IntNumEntries to provide enough queued integer operations and sufficient readiness diversity to keep five issue slots utilized. It can be bottlenecked by decodeWidth if the frontend cannot supply enough integer-class instructions to populate the queue and sustain issue.

When IntNumEntries is 16, the integer issue queue can hold up to 16 pending integer micro-ops waiting for scheduling and issue. This setting defines integer scheduling buffer capacity, including storage for operand readiness bits and age/priority metadata used by the issue logic. It impacts dispatch backpressure because the integer dispatch path stalls when the integer issue queue is full and cannot accept new integer operations. This setting requires IntIssueWidth to issue operations at a steady pace so queue occupancy can drain under sustained pressure. It can be bottlenecked by IntDispatchWidth if dispatch injects integer operations in bursts that fill the queue faster than issue can drain.

When IntNumEntries is 32, the integer issue queue can hold up to 32 pending integer micro-ops waiting for scheduling and issue. This setting defines a larger integer scheduling window that can retain many pending operations while waiting on operands or execution readiness. It impacts energy because wakeup/select logic must monitor readiness across 32 entries, and selection must arbitrate among many candidates. This setting requires numRobEntries to keep enough in-flight instructions and dependency chains alive so the integer queue has a steady supply of candidates. It can be bottlenecked by IntIssueWidth if integer operations become ready but issue throughput cannot launch them quickly enough to prevent queue congestion.

When IntNumEntries is 64, the integer issue queue can hold up to 64 pending integer micro-ops waiting for scheduling and issue. This setting defines deep buffering for integer operations and maintains readiness and dependency tracking state for up to 64 entries in the scheduler. It impacts area and power because the queue storage, wakeup network, and selection logic must scale to 64 tracked operations. This setting requires IntIssueWidth to provide enough launch bandwidth so the queue drains and does not remain saturated under sustained decode/dispatch. It can be bottlenecked by numIntPhysRegisters if rename stalls reduce the supply of integer operations entering the queue despite its capacity.

When IntDispatchWidth is 1, the dispatch stage can send up to 1 integer-class micro-op per cycle into the integer issue queue. This setting defines the intake bandwidth for integer operations into the integer scheduler and shapes how quickly integer work can be queued for execution. It impacts utilization of integer execution units because the scheduler’s pool of candidates depends on this dispatch stream. This setting requires IntNumEntries to buffer dispatched integer operations so transient readiness delays do not immediately stall dispatch. It can be bottlenecked by decodeWidth if the frontend provides decoded instructions that cannot be steered into integer dispatch due to dispatch bandwidth limits and class contention.

When IntDispatchWidth is 2, the dispatch stage can send up to 2 integer-class micro-ops per cycle into the integer issue queue. This setting defines two-per-cycle intake into the integer scheduler and enables dispatch to populate the queue with multiple integer operations each cycle. It impacts rename and scheduling pressure because dispatching two integer operations per cycle commonly allocates integer physical registers and creates dependency tracking work. This setting requires numIntPhysRegisters to supply free registers so dispatched integer destinations can be renamed without stalling. It can be bottlenecked by IntIssueWidth if the integer scheduler cannot launch issued operations quickly enough to free queue entries for continued dispatch.

When IntDispatchWidth is 3, the dispatch stage can send up to 3 integer-class micro-ops per cycle into the integer issue queue. This setting defines the integer dispatch bandwidth and allows rapid filling of the integer scheduler with ALU and branch operations. It impacts queue occupancy dynamics because the queue can fill quickly when integer dependencies delay readiness and prevent prompt issue. This setting requires IntNumEntries to provide enough capacity so dispatch can place operations while they await readiness. It can be bottlenecked by numRobEntries if completion and commit constraints restrict the supply of independent integer operations and delay freeing resources.

When IntDispatchWidth is 4, the dispatch stage can send up to 4 integer-class micro-ops per cycle into the integer issue queue. This setting defines four-per-cycle integer intake into scheduling and can drive heavy population of the integer queue under integer-dense instruction streams. It impacts execution resource demand because many dispatched integer operations will compete for issue, operand reads, and writeback bandwidth as they become ready. This setting requires IntIssueWidth to provide enough launch bandwidth so dispatched work can be issued and queue entries can be freed. It can be bottlenecked by numIntPhysRegisters and numRobEntries if rename capacity and in-flight tracking limit how many integer producers can be sustained.

When IntDispatchWidth is 5, the dispatch stage can send up to 5 integer-class micro-ops per cycle into the integer issue queue. This setting defines very high integer dispatch bandwidth and drives aggressive allocation into the integer scheduler whenever integer operations are available. It impacts backpressure because the integer queue can reach full occupancy quickly if readiness delays and issue throughput do not drain entries. This setting requires IntNumEntries and IntIssueWidth to provide enough buffering and launch capability so dispatch can remain productive without stalling frequently. It can be bottlenecked by decodeWidth if the frontend cannot supply enough integer-class decoded operations to sustain five integer dispatches per cycle.

When FpIssueWidth is 1, the floating-point issue queue is allowed to issue up to 1 FP operation per cycle to the FP execution units. This setting defines the FP scheduler’s per-cycle launch capability for FP arithmetic operations and related FP-class micro-ops. It impacts FP dependency progress because only one FP operation can begin execution each cycle, shaping how quickly FP results become available for dependent instructions. This setting requires FpNumEntries to buffer FP operations while they wait on operands and scheduling eligibility. It can be bottlenecked by FpDispatchWidth if FP operations cannot be dispatched into the FP queue at a steady rate.

When FpIssueWidth is 2, the floating-point issue queue is allowed to issue up to 2 FP operations per cycle to the FP execution units. This setting defines a dual FP launch capability that can start two eligible FP operations in the same cycle when ready. It impacts FP pipeline activity because operand reads, FP execution stages, and writeback tracking can be active for two FP operations per cycle. This setting requires numFpPhysRegisters to sustain the live FP destination set created by dispatching and executing FP-producing instructions. It can be bottlenecked by FpNumEntries if the FP queue does not hold enough ready FP operations to supply both issue slots.

When FpIssueWidth is 3, the floating-point issue queue is allowed to issue up to 3 FP operations per cycle to the FP execution units. This setting defines triple FP issue capability and drives scheduling pressure in the FP queue’s wakeup/select logic. It impacts energy because multiple FP operand reads and writebacks can occur per cycle when three FP operations are launched and complete through the pipeline. This setting requires FpDispatchWidth to deliver enough FP operations into the FP queue so the scheduler has a sustained pool of candidates. It can be bottlenecked by numRobEntries if long-latency FP operations occupy in-flight tracking and delay retirement and register reclamation.

When FpIssueWidth is 4, the floating-point issue queue is allowed to issue up to 4 FP operations per cycle to the FP execution units. This setting defines four-per-cycle FP launch capability and requires the FP scheduling and execution path to support issuing, tracking, and completing multiple FP operations each cycle. It impacts the FP register file and bypass network because multiple FP results can be produced and consumed with tight timing, increasing wiring and control activity. This setting requires FpNumEntries to buffer enough FP operations and hold readiness state so four eligible operations can be selected. It can be bottlenecked by numFpPhysRegisters if rename stalls limit the supply of FP producers and consumers entering the FP scheduler.

When FpIssueWidth is 5, the floating-point issue queue is allowed to issue up to 5 FP operations per cycle to the FP execution units. This setting defines five-per-cycle FP launch capability and drives intensive FP scheduling, operand delivery, and completion tracking activity. It impacts backend control complexity because the FP queue must select five operations, and the FP execution path must track many in-flight FP operations and writebacks. This setting requires FpDispatchWidth and FpNumEntries to provide enough queued FP operations and readiness diversity to supply five issue slots. It can be bottlenecked by decodeWidth if the instruction stream and decode pipeline do not supply enough FP-class operations to keep the FP scheduler populated.

When FpNumEntries is 16, the floating-point issue queue can hold up to 16 pending FP micro-ops waiting for scheduling and issue. This setting defines FP scheduling buffer capacity, including readiness tracking state and selection metadata for each queued FP operation. It impacts dispatch backpressure because FP dispatch stalls when the FP queue is full and cannot accept new FP-class operations. This setting requires FpIssueWidth to issue FP operations at a steady pace so queued operations drain and free entries. It can be bottlenecked by FpDispatchWidth if dispatch injects FP operations into the queue in bursts that fill 16 entries while execution readiness is delayed.

When FpNumEntries is 32, the floating-point issue queue can hold up to 32 pending FP micro-ops waiting for scheduling and issue. This setting defines a sizable FP scheduling pool and retains operand readiness and dependency tracking state across 32 entries. It impacts energy because wakeup/select logic monitors many entries for readiness changes and must arbitrate among many candidates. This setting requires numFpPhysRegisters to supply enough renameable FP registers so FP operations can be created and placed into the queue without frequent rename stalls. It can be bottlenecked by FpIssueWidth if eligible FP operations accumulate in the queue faster than they can be launched for execution.

When FpNumEntries is 64, the floating-point issue queue can hold up to 64 pending FP micro-ops waiting for scheduling and issue. This setting defines deep buffering for FP operations and holds readiness state for a large set of pending FP work. It impacts area and power because queue storage and wakeup/select circuitry must support 64 entries and their readiness updates. This setting requires FpIssueWidth to provide enough launch bandwidth so the queue drains under sustained FP pressure. It can be bottlenecked by numRobEntries if in-flight tracking and commit ordering delay retirement and cause FP operations to remain queued or live for extended periods.

When FpDispatchWidth is 1, the dispatch stage can send up to 1 FP-class micro-op per cycle into the FP issue queue. This setting defines the intake bandwidth for FP operations into FP scheduling and shapes how quickly FP work can be queued for execution. It impacts FP throughput because the FP scheduler’s candidate pool depends on this dispatch stream feeding it each cycle. This setting requires FpNumEntries to buffer dispatched FP operations while they wait on readiness and FP pipeline availability. It can be bottlenecked by decodeWidth if the frontend produces FP instructions that cannot be dispatched into the FP queue due to dispatch bandwidth and steering constraints.

When FpDispatchWidth is 2, the dispatch stage can send up to 2 FP-class micro-ops per cycle into the FP issue queue. This setting defines two-per-cycle FP intake and enables dispatch to populate the FP scheduler with multiple FP operations per cycle. It impacts rename pressure because FP-producing operations commonly allocate FP physical registers and create dependency tracking in the FP rename and issue pipeline. This setting requires numFpPhysRegisters to provide free FP registers so FP rename allocation does not stall dispatch. It can be bottlenecked by FpIssueWidth if the FP scheduler cannot launch operations quickly enough to free entries for continued dispatch.

When FpDispatchWidth is 3, the dispatch stage can send up to 3 FP-class micro-ops per cycle into the FP issue queue. This setting defines FP dispatch bandwidth and can rapidly fill the FP scheduling structure with FP arithmetic and related operations. It impacts queue occupancy because the FP issue queue can become full if readiness delays and limited FP issue prevent queued operations from draining. This setting requires FpNumEntries to provide enough queue capacity so dispatch can place operations while they wait for operand readiness. It can be bottlenecked by numFpPhysRegisters if FP rename stalls restrict the creation of FP operations that would otherwise be dispatched.

When FpDispatchWidth is 4, the dispatch stage can send up to 4 FP-class micro-ops per cycle into the FP issue queue. This setting defines four-per-cycle FP intake into scheduling and drives frequent allocation of FP queue entries and FP rename resources. It impacts execution pressure because dispatched FP operations compete for FP issue, FP pipeline stages, and writeback bandwidth as they become ready. This setting requires FpIssueWidth to provide enough launch capability so the FP queue drains and dispatch can continue without persistent stalls. It can be bottlenecked by numRobEntries if FP operation completion and commit ordering delay retirement and keep FP resources allocated.

When FpDispatchWidth is 5, the dispatch stage can send up to 5 FP-class micro-ops per cycle into the FP issue queue. This setting defines very high FP intake into scheduling and can create rapid FP queue occupancy growth under FP-dense instruction streams. It impacts backpressure because the FP dispatch path will stall when FP queue entries are exhausted and FP operations cannot be issued and completed promptly. This setting requires FpNumEntries and FpIssueWidth to provide enough buffering and FP launch capability so dispatched FP operations make steady progress. It can be bottlenecked by decodeWidth if the decode pipeline and instruction mix do not supply enough FP-class operations to sustain five FP dispatches per cycle.